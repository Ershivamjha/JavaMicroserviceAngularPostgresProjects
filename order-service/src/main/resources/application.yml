server.port: 8082
catalog.url: ${CATALOG_URL:http://localhost:8081}
spring:
  datasource: {url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/orders}, username: ${SPRING_DATASOURCE_USERNAME:orders_user}, password: ${SPRING_DATASOURCE_PASSWORD:orders_password}}
  jpa: {hibernate.ddl-auto: update, open-in-view: false}
