import {Component,Input} from '@angular/core'; import {CurrencyPipe} from '@angular/common'; import {Product} from '../../models/commerce';
@Component({selector:'app-product-catalog',standalone:true,imports:[CurrencyPipe],template:`<section><h2>Catalog service</h2><p>Products arrive through API Gateway.</p><div class="cards">@for(product of products;track product.sku){<article><span>{{product.sku}}</span><h3>{{product.name}}</h3><strong>{{product.price|currency:'USD'}}</strong></article>}@empty{<p>Loading catalog…</p>}</div></section>`}) export class ProductCatalogComponent{@Input() products:Product[]=[];}
