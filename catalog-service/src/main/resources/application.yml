server.port: 8081
spring:
  datasource: {url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/catalog}, username: ${SPRING_DATASOURCE_USERNAME:catalog_user}, password: ${SPRING_DATASOURCE_PASSWORD:catalog_password}}
  jpa: {hibernate.ddl-auto: update, open-in-view: false}
